// Exercise: strings-length-of-last-word
// Return the length of the last word in the string. Words are maximal runs of
// non-space characters; trailing spaces are ignored.
//
// Rules:
//  - `last_word_length(s)` returns the length of the final word.
//  - Scan from the end: skip trailing spaces, then count non-space characters.
//  - An empty string or all-spaces string has length 0.
//
// Only edit this file. Do not change the public interface or the tests.
#pragma once

#include <string>

inline int last_word_length(const std::string& s) {
  // TODO: from the end, skip trailing spaces, then count the last word's characters.
  (void)s;
  return 0;
}
