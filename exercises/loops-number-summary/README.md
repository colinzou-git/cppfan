# Loops: number summary

Read or receive a list of integers and compute a compact summary: minimum, maximum, sum, and even count. Empty input is represented explicitly so callers do not trust placeholder min/max values.

## Requirements

- Implement `number_summary.hpp`.
- Keep the starter buildable with C++20.
- Run `scripts/exercises/test.sh loops-number-summary` from the repo root.
