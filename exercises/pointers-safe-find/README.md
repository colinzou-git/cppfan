# Pointers: safe find

Return a pointer to the first vector element equal to a target. Practice non-owning pointers and nullptr.

## Requirements

- Implement `safe_find.hpp`.
- Keep the starter buildable with C++20.
- Run `scripts/exercises/test.sh pointers-safe-find` from the repo root.
