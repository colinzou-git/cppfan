# Input: whole-line contact parser

Parse contact rows in name,email,phone format. The parser should trim surrounding whitespace, preserve spaces inside fields, and reject malformed rows.

## Requirements

- Implement `contact_parser.hpp`.
- Keep the starter buildable with C++20.
- Run `scripts/exercises/test.sh getline-contact-parser` from the repo root.
