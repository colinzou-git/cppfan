# Functions: temperature converter

Implement temperature conversion helpers and a command dispatcher. Command C means convert Fahrenheit to Celsius. Command F means convert Celsius to Fahrenheit.

## Requirements

- Implement `temperature_converter.hpp`.
- Keep the starter buildable with C++20.
- Run `scripts/exercises/test.sh functions-temperature-converter` from the repo root.
