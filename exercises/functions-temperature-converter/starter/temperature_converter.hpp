// Exercise: functions-temperature-converter
// Implement four small, single-purpose conversion functions. The point is
// function decomposition: each formula lives in its own named function.
//
// Formulas:
//  - celsius_to_fahrenheit(c) = c * 9 / 5 + 32
//  - fahrenheit_to_celsius(f) = (f - 32) * 5 / 9
//  - celsius_to_kelvin(c)     = c + 273.15
//  - kelvin_to_celsius(k)     = k - 273.15
//
// Use floating-point division (e.g. 9.0 / 5.0), not integer division.
//
// Only edit this file. Do not change the public interface or the tests.
#pragma once

inline double celsius_to_fahrenheit(double c) {
  // TODO
  (void)c;
  return 0.0;
}

inline double fahrenheit_to_celsius(double f) {
  // TODO
  (void)f;
  return 0.0;
}

inline double celsius_to_kelvin(double c) {
  // TODO
  (void)c;
  return 0.0;
}

inline double kelvin_to_celsius(double k) {
  // TODO
  (void)k;
  return 0.0;
}
