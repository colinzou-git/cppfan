// Exercise: math-count-primes
// Return the number of prime numbers STRICTLY LESS than n.
//
// Rules:
//  - `count_primes(n)` returns how many primes are in [2, n).
//  - Use the Sieve of Eratosthenes for an efficient O(n log log n) count.
//  - n <= 2 has zero primes.
//
// Only edit this file. Do not change the public interface or the tests.
#pragma once

inline int count_primes(int n) {
  // TODO: sieve [0, n); mark composites from p*p; count the remaining primes.
  (void)n;
  return 0;
}
