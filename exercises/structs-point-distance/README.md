# Structs: point distance

Define a Point struct and compute squared Euclidean distance between two points.

## Requirements

- Implement `point_distance.hpp`.
- Keep the starter buildable with C++20.
- Run `scripts/exercises/test.sh structs-point-distance` from the repo root.
