// Exercise: strings-reverse-vowels
// Reverse only the vowels of a string, leaving every other character in place.
// Vowels are a, e, i, o, u in both cases.
//
// Rules:
//  - `reverse_vowels(s)` returns s with its vowels in reversed order.
//  - Non-vowel characters keep their positions.
//  - Two pointers from the ends, swapping vowel pairs, is the intended approach.
//
// Only edit this file. Do not change the public interface or the tests.
#pragma once

#include <string>

inline std::string reverse_vowels(std::string s) {
  // TODO: two pointers; find a vowel from each end, swap, and move inward.
  return s;
}
