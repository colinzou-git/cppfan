# Operators: normalized fraction

Implement a Fraction value type with normalization, addition, equality, and stream insertion.

## Requirements

- Implement `fraction.hpp`.
- Keep the starter buildable with C++20.
- Run `scripts/exercises/test.sh operators-fraction-normalize` from the repo root.
