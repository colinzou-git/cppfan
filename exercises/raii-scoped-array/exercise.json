{
  "id": "raii-scoped-array",
  "title": "RAII: a move-only scoped array",
  "skill_ids": ["cpp.raii.resource_lifetime", "cpp.raii.destructor_cleanup", "cpp.value_semantics.move"],
  "difficulty": "intermediate",
  "estimated_minutes": 30,
  "editable_files": ["scoped_array.hpp"],
  "build_command": "g++ -std=c++20 -Wall -Wextra -Wpedantic -fsanitize=address,undefined -I exercises/_harness -I exercises/raii-scoped-array/work exercises/raii-scoped-array/tests/tests.cpp -o exercises/raii-scoped-array/work/run",
  "test_command": "exercises/raii-scoped-array/work/run",
  "required_tests": ["test_basic_storage", "test_live_count_returns_to_baseline", "test_move_transfers_ownership"],
  "hints": [
    "Allocate with `new int[size]()` so elements start at 0; free with `delete[]`.",
    "Increment a static counter in the constructor and decrement it in the destructor — but only when this object still owns memory.",
    "After moving, set the source's pointer to nullptr and size to 0 so its destructor does not double-free or double-decrement."
  ],
  "project_lab": "note-manager"
}
