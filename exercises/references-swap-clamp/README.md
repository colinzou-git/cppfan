# References: swap and clamp

Practice reference parameters by swapping two integers and clamping a value in place.

## Requirements

- Implement `swap_clamp.hpp`.
- Keep the starter buildable with C++20.
- Run `scripts/exercises/test.sh references-swap-clamp` from the repo root.
