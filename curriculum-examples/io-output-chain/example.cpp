// Positive example for cpp.program_basics.io.lesson.
// std::cout << value prints; << chains left to right, so a label, a value, and a
// newline compose into one statement.
#include <iostream>

int main() {
    int x = 5;
    std::cout << "x = " << x << "\n";
    return 0;
}
