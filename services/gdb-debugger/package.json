{
  "name": "cppfan-gdb-debugger",
  "version": "0.1.0",
  "private": true,
  "description": "OVH-hosted GDB/MI debugger service for the cppFan Code Lab (#442). Compiles and debugs untrusted C++ in an isolated sandbox; the Next.js app proxies to it.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "node --experimental-strip-types src/server.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^4.1.0"
  }
}
